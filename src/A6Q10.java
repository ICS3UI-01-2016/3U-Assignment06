
import java.util.Scanner;

/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
/**
 *
 * @author petet9087
 */
public class A6Q10 {

    /**
     * @param args the command line arguments
     */
    public static boolean sameDashes(String first, String second) {
        // start count loop
        int count = 0;
       //
        if (count > length){
            
        }
        
       return;
    }
    public static void main(String[] args) {
        // create a new scanner
        Scanner input = new Scanner(System.in);
        // ask user for a word or number with dashes
        System.out.println("Please enter the first word or number with dashes");
        // store user input with a variable
        String first = input.nextLine();
        // ask user for a second number or word with dashes
        System.out.println("Please enter the second number or word with dashes");
        // store user input with a variable
        String second = input.nextLine();
        // link the above into the loop
        sameDashes (first, second);
        
    }
}
